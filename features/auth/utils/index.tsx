const storeAuthCredentials = () => {};

const saveAuthToken = (token: string) => {};

const prefillAuthCredentials = () => {}

const saveFirebaseToken = (token: string) => {}

export { saveAuthToken, saveFirebaseToken, storeAuthCredentials, prefillAuthCredentials };
